<!doctype html>
<html lang="en">
  <head>
    <title>Shop | REVIVO</title>
    <link href="/css/styles.css" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Henny+Penny&family=Playfair+Display:wght@400;500;600&family=Montserrat:wght@300;400;500&display=swap" rel="stylesheet">

    <style>
      .category-filter {
        scrollbar-width: none; 
        -ms-overflow-style: none;
      }
      .category-filter::-webkit-scrollbar {
        display: none; 
      }
      .product-card {
        transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        background-color: white;
      }
      .product-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
      }
      .section-heading {
        font-family: 'Playfair Display', serif;
        font-weight: 500;
        letter-spacing: 0.05em;
        position: relative;
        display: inline-block;
      }
      .section-heading::after {
        content: '';
        position: absolute;
        bottom: -8px;
        left: 0;
        width: 60px;
        height: 2px;
        background-color: #f8c78d;
        transition: width 0.3s ease;
      }
      .section-heading:hover::after {
        width: 100%;
      }
      .hero-heading {
        font-family: 'Henny Penny', cursive;
        font-weight: 400;
        letter-spacing: 0.05em;
        text-shadow: 1px 1px 3px rgba(0,0,0,0.2);
      }
      .hero-subheading {
        font-family: 'Montserrat', sans-serif;
        letter-spacing: 0.1em;
        font-weight: 300;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
      }
      .hero-section {
        background: linear-gradient(135deg, #2C2C2C 0%, #1a1a1a 100%);
        position: relative;
      }
      .clip-path-wave {
        clip-path: polygon(0% 0%, 4% 40%, 13% 60%, 20% 35%, 33% 60%, 44% 30%, 52% 55%, 65% 45%, 78% 65%, 85% 25%, 95% 50%, 100% 0%, 100% 100%, 0% 100%);
      }
      .font-playfair {
        font-family: 'Playfair Display', serif;
      }
      .font-montserrat {
        font-family: 'Montserrat', sans-serif;
      }
      html {
        scroll-behavior: smooth;
      }
      @media (max-width: 768px) {
        .clip-path-wave {
          height: 6px;
        }
      }
    </style>
  </head>

  <body class="bg-[#f8f7f5]">

    <%- include('../partials/user/header.ejs') %>

    <section class="relative py-24 hero-section text-white overflow-hidden">
      <div class="absolute inset-0" style="background-image: url('/Images/images/OLD_PAPER_TEXTURE.jpg'); opacity: 0.2; background-size: cover;"></div>
      <div class="absolute inset-0 bg-gradient-to-r from-[#1a1a1a]/90 to-transparent"></div>
      
      <div class="container mx-auto px-4 relative z-10">
        <div class="flex flex-col md:flex-row items-center justify-between">
          <div class="text-left max-w-xl mb-8 md:mb-0">
            <h4 class="text-[#f8c78d] text-sm tracking-widest uppercase mb-2 hero-subheading">Curated Collection</h4>
            <h1 class="text-5xl md:text-6xl hero-heading mb-6 leading-tight">Discover <span class="text-[#f8c78d]">Vintage</span> Treasures</h1>
            <p class="text-[#ffffff]/80 hero-subheading mb-8 text-lg">Each piece tells a story, each design has a history. Find your perfect vintage statement piece today.</p>
            <div class="flex flex-wrap gap-4">
              <a href="#featured-collection" class="px-8 py-3 bg-[#f8c78d] text-[#2C2C2C] rounded-full hover:bg-[#e5b67d] transition-all font-montserrat font-medium tracking-wider">Explore Collection</a>
              <a href="/new-arrivals" class="px-8 py-3 bg-transparent border border-white/30 text-white rounded-full hover:bg-white/10 transition-all font-montserrat font-medium tracking-wider">New Arrivals</a>
            </div>
          </div>
          
          <div class="relative hidden md:block">
            <div class="w-80 h-80 rounded-full bg-[#f8c78d]/20 backdrop-blur-sm relative overflow-hidden border border-white/10">
              <div class="absolute -right-20 -bottom-20 w-64 h-64 rounded-full bg-[#f8c78d]/30"></div>
              <div class="absolute left-10 top-10 w-16 h-16 rounded-full bg-white/20"></div>
            </div>
            <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2">
              <svg class="w-16 h-16 text-white/70" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1">
                <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"></path>
              </svg>
            </div>
          </div>
        </div>
      </div>
      
      <div class="absolute bottom-0 left-0 w-full h-8 bg-[#f8f7f5] clip-path-wave"></div>
      <div class="absolute -bottom-6 right-12 transform rotate-45 w-24 h-24 border border-[#f8c78d]/30 rounded-md"></div>
      <div class="absolute top-12 right-12 w-8 h-8 rounded-full bg-[#f8c78d]/40"></div>
    </section>

    <section class="py-16" id="featured-collection">
      <div class="container mx-auto px-4">
        <div class="relative mb-16 rounded-lg overflow-hidden shadow-lg">
          <div class="absolute inset-0 bg-gradient-to-r from-[#2C2C2C]/90 to-[#2C2C2C]/50" style="z-index: 1;"></div>
          <div class="absolute inset-0" style="background-image: url('/Images/images/OLD_PAPER_TEXTURE.jpg'); opacity: 0.1; background-size: cover;"></div>
          <div class="relative z-10 flex flex-col md:flex-row items-center justify-between p-8 md:p-12">
            <div class="text-white mb-6 md:mb-0">
              <h3 class="text-sm font-montserrat tracking-widest text-[#f8c78d] uppercase mb-2">This Month</h3>
              <h2 class="text-3xl mb-4 font-playfair">Featured Collection</h2>
              <p class="text-white/80 max-w-md mb-6">Explore our carefully curated selection of rare and unique vintage finds from around the world.</p>
              <a href="#featured" class="inline-block px-6 py-2 border-b-2 border-[#f8c78d] text-[#f8c78d] hover:text-white hover:border-white transition-colors">View Featured</a>
            </div>
            <div class="flex space-x-2">
              <div class="w-16 h-16 md:w-24 md:h-24 rounded-lg bg-white/10 backdrop-blur-sm flex items-center justify-center border border-white/20 transform rotate-3">
                <span class="text-2xl md:text-3xl text-white font-playfair">20%</span>
              </div>
              <div class="w-16 h-16 md:w-24 md:h-24 rounded-lg bg-[#f8c78d]/20 backdrop-blur-sm flex items-center justify-center border border-[#f8c78d]/30 transform -rotate-3">
                <span class="text-xs md:text-sm text-white font-montserrat tracking-wider text-center">LIMITED<br>OFFER</span>
              </div>
            </div>
          </div>
        </div>
        
        <div class="mb-16">
          <div class="flex items-center justify-between mb-8">
            <div>
              <h4 class="text-[#f8c78d] text-xs tracking-widest uppercase mb-2 font-montserrat">Browse By</h4>
              <h2 class="text-3xl section-heading text-[#2C2C2C]">Our Collections</h2>
            </div>
            <div class="flex items-center gap-2">
              <span class="text-sm text-[#2C2C2C]/70 font-montserrat">Filter:</span>
              <select id="mobile-category-filter" class="md:hidden border border-[#e0dcd4] bg-white px-3 py-2 rounded-full text-sm font-montserrat focus:ring-2 focus:ring-[#f8c78d]/30 focus:outline-none">
                <option value="all">All Categories</option>
                <% categories.forEach(category => { %>
                  <option value="<%= category._id %>" <%= selectedCategory === category._id.toString() ? 'selected' : '' %>>
                    <%= category.name %>
                  </option>
                <% }); %>
              </select>
            </div>
          </div>
          
          <div class="hidden md:block">
            <div class="flex gap-3 overflow-x-auto pb-2 category-filter">
              <a href="/shop" 
                 class="whitespace-nowrap px-6 py-3 rounded-full border font-montserrat text-sm transition-all duration-300 <%= selectedCategory === 'all' ? 'bg-[#2C2C2C] text-white border-[#2C2C2C] shadow-md' : 'bg-white border-[#e0dcd4] text-[#2C2C2C] hover:bg-[#f8c78d]/10 hover:border-[#f8c78d]/30' %>">
                All Categories
              </a>
              <% categories.forEach(category => { %>
                <a href="/shop?category=<%= category._id %>" 
                   class="whitespace-nowrap px-6 py-3 rounded-full border font-montserrat text-sm transition-all duration-300 <%= selectedCategory === category._id.toString() ? 'bg-[#2C2C2C] text-white border-[#2C2C2C] shadow-md' : 'bg-white border-[#e0dcd4] text-[#2C2C2C] hover:bg-[#f8c78d]/10 hover:border-[#f8c78d]/30' %>">
                  <%= category.name %>
                </a>
              <% }); %>
            </div>
          </div>
        </div>

        <!-- Filter Section -->
        <div class="mb-12 bg-white p-6 rounded-lg shadow-sm border border-[#e0dcd4]/50">
          <div class="flex justify-between items-center mb-4">
            <h3 class="text-lg font-playfair text-[#2C2C2C]">Filter Products</h3>
            <button id="toggleFilters" class="md:hidden text-[#2C2C2C] hover:text-[#f8c78d] focus:outline-none">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
              </svg>
            </button>
          </div>

          <form id="filterForm" class="filter-container hidden md:grid grid-cols-1 md:grid-cols-4 gap-4">
            <!-- Category Filter -->
            <div>
              <label class="block text-sm font-montserrat text-[#2C2C2C]/70 mb-2">Category</label>
              <select name="category" class="w-full border border-[#e0dcd4] p-2 rounded-md focus:ring-2 focus:ring-[#f8c78d]/30 focus:outline-none">
                <option value="">All Categories</option>
                <% categories.forEach(category => { %>
                  <option value="<%= category._id %>" <%= filters.category === category._id.toString() ? 'selected' : '' %>>
                    <%= category.name %>
                  </option>
                <% }); %>
              </select>
            </div>

            <!-- Brand Filter -->
            <div>
              <label class="block text-sm font-montserrat text-[#2C2C2C]/70 mb-2">Brand</label>
              <select name="brand" class="w-full border border-[#e0dcd4] p-2 rounded-md focus:ring-2 focus:ring-[#f8c78d]/30 focus:outline-none">
                <option value="">All Brands</option>
                <% brands.forEach(brand => { %>
                  <option value="<%= brand._id %>" <%= filters.brand === brand._id.toString() ? 'selected' : '' %>>
                    <%= brand.brandName %>
                  </option>
                <% }); %>
              </select>
            </div>

            <!-- Size Filter -->
            <div>
              <label class="block text-sm font-montserrat text-[#2C2C2C]/70 mb-2">Size</label>
              <select name="size" class="w-full border border-[#e0dcd4] p-2 rounded-md focus:ring-2 focus:ring-[#f8c78d]/30 focus:outline-none">
                <option value="">All Sizes</option>
                <option value="S" <%= filters.size === 'S' ? 'selected' : '' %>>S</option>
                <option value="M" <%= filters.size === 'M' ? 'selected' : '' %>>M</option>
                <option value="L" <%= filters.size === 'L' ? 'selected' : '' %>>L</option>
                <option value="XL" <%= filters.size === 'XL' ? 'selected' : '' %>>XL</option>
              </select>
            </div>

            <!-- Condition Filter -->
            <div>
              <label class="block text-sm font-montserrat text-[#2C2C2C]/70 mb-2">Condition</label>
              <select name="condition" class="w-full border border-[#e0dcd4] p-2 rounded-md focus:ring-2 focus:ring-[#f8c78d]/30 focus:outline-none">
                <option value="">All Conditions</option>
                <option value="Like New" <%= filters.condition === 'Like New' ? 'selected' : '' %>>Like New</option>
                <option value="Gently Used" <%= filters.condition === 'Gently Used' ? 'selected' : '' %>>Gently Used</option>
                <option value="Fair Condition" <%= filters.condition === 'Fair Condition' ? 'selected' : '' %>>Fair Condition</option>
              </select>
            </div>

            <!-- Heritage Filter -->
            <div>
              <label class="block text-sm font-montserrat text-[#2C2C2C]/70 mb-2">Heritage</label>
              <select name="heritage" class="w-full border border-[#e0dcd4] p-2 rounded-md focus:ring-2 focus:ring-[#f8c78d]/30 focus:outline-none">
                <option value="">All Heritage</option>
                <option value="Prime Layers" <%= filters.heritage === 'Prime Layers' ? 'selected' : '' %>>Prime Layers</option>
                <option value="Vintage Athletics" <%= filters.heritage === 'Vintage Athletics' ? 'selected' : '' %>>Vintage Athletics</option>
                <option value="Y2K Essentials" <%= filters.heritage === 'Y2K Essentials' ? 'selected' : '' %>>Y2K Essentials</option>
              </select>
            </div>

            <!-- Price Range -->
            <div class="md:col-span-2">
              <label class="block text-sm font-montserrat text-[#2C2C2C]/70 mb-2">Price Range</label>
              <div class="flex items-center gap-2">
                <input type="number" name="minPrice" placeholder="Min" value="<%= filters.minPrice || '' %>" 
                       class="w-1/2 border border-[#e0dcd4] p-2 rounded-md focus:ring-2 focus:ring-[#f8c78d]/30 focus:outline-none">
                <span>-</span>
                <input type="number" name="maxPrice" placeholder="Max" value="<%= filters.maxPrice || '' %>" 
                       class="w-1/2 border border-[#e0dcd4] p-2 rounded-md focus:ring-2 focus:ring-[#f8c78d]/30 focus:outline-none">
              </div>
            </div>

            <!-- Buttons -->
            <div class="md:col-span-4 flex gap-4">
              <button type="submit" class="px-6 py-2 bg-[#2C2C2C] text-white rounded-md hover:bg-[#1a1a1a] font-montserrat transition-colors">
                Apply Filters
              </button>
              <a href="/shop" class="px-6 py-2 border border-[#e0dcd4] text-[#2C2C2C] rounded-md hover:bg-[#f8c78d]/10 font-montserrat transition-colors">
                Clear Filters
              </a>
            </div>
          </form>
        </div>

        <div class="relative">
          <div class="absolute -top-8 -left-8 w-16 h-16 border border-[#f8c78d]/20 rounded-full hidden lg:block"></div>
          <div class="absolute -bottom-8 -right-8 w-24 h-24 border-2 border-[#f8c78d]/10 rounded-full hidden lg:block"></div>
          
          <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
            <% if (products && products.length > 0) { %>
              <% products.forEach(product => { %>
                <div class="product-card group bg-white border border-[#e0dcd4]/50 p-4 hover:shadow-lg transition-all duration-300 rounded-md relative overflow-hidden">
                  <% if (product.isNew) { %>
                    <div class="absolute top-4 right-4 bg-[#f8c78d] text-[#2C2C2C] text-xs font-medium py-1 px-3 rounded-full z-10">New</div>
                  <% } %>
                  
                  <div class="relative aspect-[1/1] overflow-hidden mb-4 bg-white/20 group-hover:shadow-md transition-all">
                    <a href="/product/<%= product._id %>">
                      <img 
                        src="<%= product.productImage && product.productImage[0] ? product.productImage[0] : '/Images/placeholder.jpg' %>" 
                        alt="<%= product.productName %>" 
                        class="w-full h-full object-contain p-2 group-hover:scale-105 transition-transform duration-500"
                        onerror="this.src='/Images/placeholder.jpg'"
                      >
                    </a>
                    <div class="absolute inset-0 bg-[#2C2C2C]/60 opacity-0 group-hover:opacity-100 transition-opacity flex items-center justify-center">
                      <a href="/product/<%= product._id %>" class="px-4 py-2 bg-white text-[#2C2C2C] rounded-full text-xs font-montserrat hover:bg-[#f8c78d] transition-colors">Quick View</a>
                    </div>
                  </div>
                  
                  <div class="px-2">
                    <div class="mb-2">
                      <span class="text-xs text-[#2C2C2C]/60 font-montserrat"><%= product.category?.name || 'Vintage' %></span>
                    </div>
                    
                    <h3 class="text-[#333] font-medium mb-2 uppercase text-sm tracking-wider font-montserrat">
                      <a href="/product/<%= product._id %>" class="hover:text-[#f8c78d] transition-colors"><%= product.productName %></a>
                    </h3>
                    
                    <div class="flex items-center justify-between">
                      <div>
                        <p class="text-[#333] text-sm font-montserrat">$<%= product.salesPrice %> $</p>
                        <% if (product.originalPrice) { %>
                          <p class="text-[#333]/60 text-xs font-montserrat line-through">$<%= product.originalPrice %> $</p>
                        <% } %>
                      </div>
                      <button 
                        onclick="addToCart('<%= product._id %>')"
                        class="w-10 h-10 flex items-center justify-center bg-[#2C2C2C] text-white rounded-full hover:bg-[#f8c78d] hover:text-[#2C2C2C] transition-colors transform hover:scale-110"
                        title="Add to cart"
                      >
                        <svg class="w-5 h-5" viewBox="0 0 24 24" fill="none" stroke="currentColor">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" />
                        </svg>
                      </button>
                    </div>
                  </div>
                </div>
              <% }); %>
            <% } else { %>
              <div class="col-span-full text-center py-12">
                <div class="max-w-md mx-auto">
                  <svg class="w-16 h-16 mx-auto text-[#2C2C2C]/30 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/>
                  </svg>
                  <h3 class="text-xl font-medium text-[#2C2C2C] mb-2 section-heading">No Products Found</h3>
                  <p class="text-[#2C2C2C]/70 mb-6 font-montserrat">We couldn't find any products matching your criteria.</p>
                  <a href="/shop" class="inline-block px-6 py-3 bg-[#2C2C2C] text-white rounded-md hover:bg-[#1a1a1a] transition-colors font-montserrat">
                    Browse All Products
                  </a>
                </div>
              </div>
            <% } %>
          </div>
        </div>
        
        <% if (totalPages > 1) { %>
        <div class="mt-16 flex justify-center">
          <div class="inline-flex rounded-md shadow-sm">
            <% if (hasPreviousPage) { %>
              <a href="/shop?page=<%= previousPage %>&<%= new URLSearchParams(filters).toString() %>" 
                 class="px-4 py-2 text-sm font-montserrat border border-[#e0dcd4] rounded-l-md hover:bg-[#f8c78d]/10 text-[#2C2C2C]">
                Previous
              </a>
            <% } else { %>
              <span class="px-4 py-2 text-sm font-montserrat border border-[#e0dcd4] rounded-l-md text-[#2C2C2C]/50 cursor-not-allowed">
                Previous
              </span>
            <% } %>

            <% for (let i = 1; i <= totalPages; i++) { %>
              <% if (i === currentPage) { %>
                <span class="px-4 py-2 text-sm font-montserrat border-t border-b border-[#e0dcd4] bg-[#2C2C2C] text-white">
                  <%= i %>
                </span>
              <% } else { %>
                <a href="/shop?page=<%= i %>&<%= new URLSearchParams(filters).toString() %>" 
                   class="px-4 py-2 text-sm font-montserrat border-t border-b border-[#e0dcd4] hover:bg-[#f8c78d]/10 text-[#2C2C2C]">
                  <%= i %>
                </a>
              <% } %>
            <% } %>

            <% if (hasNextPage) { %>
              <a href="/shop?page=<%= nextPage %>&<%= new URLSearchParams(filters).toString() %>" 
                 class="px-4 py-2 text-sm font-montserrat border border-[#e0dcd4] rounded-r-md hover:bg-[#f8c78d]/10 text-[#2C2C2C]">
                Next
              </a>
            <% } else { %>
              <span class="px-4 py-2 text-sm font-montserrat border border-[#e0dcd4] rounded-r-md text-[#2C2C2C]/50 cursor-not-allowed">
                Next
              </span>
            <% } %>
          </div>
        </div>
        <% } %>
      </div>
    </section>

    <%- include('../partials/user/footer.ejs') %>

    <script>
      // Toggle filters on mobile
      const toggleButton = document.getElementById('toggleFilters');
      const filterContainer = document.querySelector('.filter-container');

      toggleButton.addEventListener('click', () => {
        filterContainer.classList.toggle('hidden');
      });

      // Handle filter form submission
      document.getElementById('filterForm').addEventListener('submit', function(e) {
        e.preventDefault();
        const formData = new FormData(this);
        const params = new URLSearchParams(formData).toString();
        window.location.href = `/shop?${params}`;
      });

      // Mobile category filter
      document.getElementById('mobile-category-filter')?.addEventListener('change', function() {
        const categoryId = this.value;
        if (categoryId === 'all') {
          window.location.href = '/shop';
        } else {
          window.location.href = `/shop?category=${categoryId}`;
        }
      });

      function showToast(message, type = 'success') {
        const toast = document.createElement('div');
        toast.className = `fixed bottom-4 right-4 ${type === 'success' ? 'bg-[#2C2C2C]' : 'bg-red-500'} text-white px-6 py-3 rounded-lg shadow-xl transform translate-y-0 opacity-100 transition-all duration-300 z-50 flex items-center`;
        
        const icon = document.createElement('span');
        icon.className = 'mr-2';
        icon.innerHTML = type === 'success' 
          ? '<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path></svg>'
          : '<svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path></svg>';
        
        const text = document.createElement('span');
        text.textContent = message;
        text.className = 'font-montserrat';
        
        toast.appendChild(icon);
        toast.appendChild(text);
        document.body.appendChild(toast);

        setTimeout(() => {
          toast.classList.add('translate-y-full', 'opacity-0');
          setTimeout(() => toast.remove(), 300);
        }, 3000);
      }
      
      async function addToCart(productId) {
        try {
          const response = await fetch('/add-to-cart', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify({ productId }),
          });

          const data = await response.json();

          if (data.success) {
            showToast('Added to cart successfully!', 'success');
          } else {
            throw new Error(data.message || 'Failed to add to cart');
          }
        } catch (error) {
          console.error('Error adding to cart:', error);
          showToast(error.message || 'Failed to add to cart', 'error');
        }
      }
    </script>
  </body>
</html>
